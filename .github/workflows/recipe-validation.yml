name: recipe validation

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  recipe-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: introspection-org/recipes-action@v0
